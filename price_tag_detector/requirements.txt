# Price Tag Detector - Python dependencies
# PyTorch is installed separately via install.bat (CUDA build)
# These are installed after PyTorch

ultralytics==8.2.18
opencv-python==4.9.0.80
numpy==1.26.4
lapx>=0.5.2
